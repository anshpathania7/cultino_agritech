name: cultino_agritech
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  dio: ^4.0.0
  dio_http_cache: ^0.3.0
  flutter:
    sdk: flutter
  
  get: ^4.3.8
  get_storage: ^2.0.3
  image_picker: ^0.8.4
  octo_image: ^1.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  #assets:
  # - assets/images/
